import React from "react";

const Navbar = () => {
  return (
    <nav
      style={{
        background: "#0f766e",
        color: "white",
        padding: "15px",
      }}
    >
      <h2>Photo Studio Management</h2>
    </nav>
  );
};

export default Navbar;
